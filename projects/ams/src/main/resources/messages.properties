# Globalny komunikat (domyślny) dla adnotacji NotBlank
NotBlank=Cannot be an empty text

# Komunikat (domyślny) dla adnotacji NotBlank na polach o nazwie password
NotBlank.password=Password cannot be an empty text

# Komunikat (domyślny) dla adnotacji NotBlank na polach o nazwie password w obiektach modelu o nazwie registration
NotBlank.registration.password=Password for new user cannot be an empty text

# Komunikat (domyślny) dla adnotacji Size używanej nad polami tekstowymi (o typie String)
Size.java.lang.String=Field {0} must be greater than {2} and less than {1}.

# Komunikat (domyślny) dla adnotacji Size używanej nad polami będącymi listami (o typie java.util.List)
Size.java.util.List=List must have more than {1} and less than {0} elements

NotNull.java.lang.Number=Number must be provided
StrongPassword=We required a strong password with at least: one digit, one small letter, one big letter, one special character from [!@#$%] with length at least 3 characters and no more than 12